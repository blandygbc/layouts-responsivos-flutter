import 'package:flutter/material.dart';

class DrinksMenu extends StatelessWidget {
  const DrinksMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Center(child: Text('Drinks Menu')),
    );
  }
}
